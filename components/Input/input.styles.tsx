import { StyleSheet } from "react-native";

const InputStyles = StyleSheet.create({
  container: {
    width: 300,
    height: 40,
    backgroundColor: "#fff",
    marginBottom: 50,
  },
});

export default InputStyles;
